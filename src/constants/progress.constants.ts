const PROGRESS = {
  /** A video auto-completes once at least this percentage is watched. */
  COMPLETION_THRESHOLD_PERCENT: 90,
  MIN_PERCENT: 0,
  MAX_PERCENT: 100,
} as const;

export { PROGRESS };
